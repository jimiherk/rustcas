{
    "name": "pnpm-workspace",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "build-wasm": "wasm-pack build --out-dir ui/wasm --out-name index",
        "build-ui": "npm run build --prefix ui",
        "build": "npm run build-wasm && npm run build-ui"
    }
}